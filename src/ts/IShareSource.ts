module Sharing {
    export interface IShareSource {
        label: string;
        link: string;
        imageSource: string;
        largeImageSource: string;
        popupHeight: number;
        popupWidth: number;
        icon: string;
    }
}